#pragma once

// SDDMM zone configuration stub.
// Will be filled in when device-side algorithms are implemented.

#include <map>
#include <string>

namespace sddmm_zone_config {

inline std::map<std::string, std::string> get_zone_defines() {
    return {};
}

} // namespace sddmm_zone_config
